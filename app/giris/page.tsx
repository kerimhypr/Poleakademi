"use client";
import Link from "next/link";
import { useActionState } from "react";
import { signIn } from "@/app/auth/actions";
export default function SignInPage() { const [state, action, pending] = useActionState(signIn, {}); return <main className="shell flex min-h-[calc(100vh-4rem)] items-center justify-center py-12"><form action={action} className="panel w-full max-w-md p-7"><h1 className="font-serif text-3xl text-paper">Tekrar hoş geldin</h1><p className="mt-2 text-sm text-zinc-500">Düşünceye kaldığın yerden devam et.</p><div className="mt-7 space-y-4"><input name="email" className="field" type="email" placeholder="E-posta" required /><input name="password" className="field" type="password" placeholder="Şifre" required />{state.error && <p className="text-sm text-red-400">{state.error}</p>}<button disabled={pending} className="button w-full">{pending ? "Giriş yapılıyor…" : "Giriş yap"}</button></div><p className="mt-4 text-right text-sm"><Link className="text-zinc-400 hover:text-amber" href="/sifremi-unuttum">Şifremi unuttum</Link></p><p className="mt-4 text-center text-sm text-zinc-500">Hesabın yok mu? <Link className="text-amber" href="/kayit">Kayıt ol</Link></p></form></main>; }
